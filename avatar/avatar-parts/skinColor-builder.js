import React from 'react';
import {Text, View, Image} from 'react-native';

// WORK IN PROGRESS
const SkinColorBuilder = props => {
    return (
      <View>

      </View>
    );
  };

export default SkinColorBuilder;